# research/parity/

Cloud-local + engine-oracle parity diagnostics — the first-divergence walks.

- **Holds:** parity diffs and validation studies (e.g. `b0d_parity_diff_feb2025.md`, `step_a_parity_2025-06.md`, `backtest-validation-results.md`, `first-divergence-diff-2025.md`, `residual-root-cause-2025.md` + the `artifacts/` provenance).
- **Goes here:** "where do cloud and local (or engine and oracle) diverge, and why" — the mechanical root-cause diffs (the #182 protocol).
- **Does NOT:** experiment writeups (→ `../experiments/`), validation-gate methodology (→ `../methodology/`), or the numeric ledger (→ `results/`).
