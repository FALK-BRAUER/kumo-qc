# sweeps/grids/

Permutation specifications — TRACKED. Each defines a parameter/phase grid the driver expands into N strategy variants.

- **Holds:** `<sweep-name>.yaml` — param ranges, phase swaps, the base strategy to perturb.
- **Goes here:** declarative sweep definitions.
- **Does NOT:** results (that's `reports/`), generated runs (`runs/`).
