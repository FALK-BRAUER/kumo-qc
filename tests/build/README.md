# tests/build

Unit tests for build and dist-generation code.
Use this directory for fast checks that generated `dist/main.py`, manifests, metadata, and flat imports round-trip correctly.
Do not put long-running LEAN backtests, market-data fixtures, or generated build artifacts here.
