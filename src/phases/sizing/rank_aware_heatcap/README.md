# rank_aware_heatcap

Scanner-rank-aware sizing phase for opt-in LambdaMART experiments.
It keeps the committed-cash heat-cap behavior and only changes per-symbol target size by frozen scanner rank.
Do not put model training, OCR/watchlist labels, or report artifacts in this folder.
