George-style ranking reorders already-qualified BCT candidates.
It belongs after `signal` and before entry-selection in experimental scanner-alignment configs.
It does not qualify new names, size orders, submit orders, or read generated research CSVs.
It uses only QC-safe raw chart state from maintained indicators and the daily tracker.
Do not add George/BCT transcript, OCR, learned score, or injected research-score dependencies here.
