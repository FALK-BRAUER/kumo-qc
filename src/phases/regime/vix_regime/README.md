# vix_regime

Scenario B regime module for the #386 architecture proof.
It gates new entries when the current VIX level is above the configured risk-off threshold.
This folder should contain only the phase implementation and local module notes.
Runtime VIX subscription or history wiring belongs in runtime code, not here.
