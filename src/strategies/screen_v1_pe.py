"""#340-C screen V1 — flat $200 add (Pe), the CONTROL (S1 + StagedRiskPyramid via the shared factory)."""
from __future__ import annotations

from strategies.pyramid_screen import make_config

CONFIG = make_config("Pe")
LEAN_ENTRY = True
