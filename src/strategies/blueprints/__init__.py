"""#386 scenario blueprints — the A/B/C modular-proof configs (scenario_a/b/c.py).

Each is a {slot: module(params)} StrategyConfig composing the #254 catalog modules across the
two-clock day/intraday phase pipeline. The PROOF: engine + framework code byte-identical across A/B/C
(any engine edit to make a scenario work = modularization failed); only the config maps differ.
"""
